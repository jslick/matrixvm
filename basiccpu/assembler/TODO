* Add assembler directive 'defines' so that constants don't have to be
  hardcoded in assembler.ypp
* See also TODOs noted in source code
  * Some instructions should be able to accept more argument types
